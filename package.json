{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "tsup": "^6.2.3",
    "turbo": "^1.5.5",
    "typescript": "^4.8.4"
  }
}
